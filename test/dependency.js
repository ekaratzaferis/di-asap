/**
 * Returns some kind of data, does not have dependencies.
 * @type {{some: string}}
 */
module.exports = 'This string was required via dependency injection (DI)! It is also tested via the \'should\' npm package, which again is required via DI for us!'
